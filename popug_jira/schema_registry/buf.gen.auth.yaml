version: v1
managed:
  enabled: true
  go_package_prefix:
    default: github.com/lyckety/async_arch/popug_jira/services/auth/pkg/grpc

plugins:
  - name: go
    out: .
    opt: paths=source_relative
  - name: go-grpc
    out: .
    opt: paths=source_relative
