version: v1
managed:
  enabled: true
  go_package_prefix:
    default: github.com/lyckety/async_arch/popug_jira/schema-registry/pkg/pb

plugins:
  - name: go
    out: .
    opt: paths=source_relative
  - name: go-grpc
    out: .
    opt: paths=source_relative
